<% provide(:title, 'Voluntarios') %>

<div class="row">
  <div class="col-md-2">
    <h2>Voluntarios</h2>
  </div>
  <div class="col-md-8" id="divBtnNuevoVoluntario">
    <%= form_tag volunteers_path, method: "get", class: "form-inline formBusqueda" do %>
        <%= hidden_field_tag :direction, params[:direction]  %>
        <%= hidden_field_tag :sort, params[:sort]  %>
        <p>
          <%= text_field_tag :search, params[:search], class:'form-control', placeholder:'Buscar', id:'inputBuscar' %>
          <%= submit_tag "Buscar", class:'btn btn-default', name:nil %>
        </p>
    <% end %>
  </div>
</div>
<div class="row">
</div>
<div id="volunteers">
  <%= render 'volunteers' %>
</div>
