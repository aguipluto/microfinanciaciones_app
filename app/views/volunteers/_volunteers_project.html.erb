<% if proyecto.volunteers.count > 0 %>
    <bold><%= pluralize(proyecto.volunteers.count, 'voluntario inscrito', 'voluntarios inscritos') %></bold>
<% elsif proyecto.aportable? %>
    Sé el primer voluntario!
<% end %>