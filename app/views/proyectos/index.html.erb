<% provide(:title, 'Proyectos') %>

<div class="row">
  <div class="col-sm-2">
    <h2>Proyectos</h2>
  </div>
  <div class="col-sm-4" id="divBtnNuevoProyecto">
    <% if signed_in? && current_user.admin? %>
        <%= link_to "Nuevo proyecto", new_proyecto_path, class: 'btn btn-primary', id: 'btnNuevoProyecto' %>
    <% end %>
  </div>
</div>

<div id="proyectos">
  <%= render partial: 'proyectos' %>
</div>
